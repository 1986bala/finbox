<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:ads="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FFFFFF">

    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!--
      AdMob adaptive banner.
      ad-unit-id below is Google's PUBLIC TEST banner unit — safe to ship for testing.
      Replace with your own ad unit ID (created in admob.google.com) before publishing.
      Test ID reference: https://developers.google.com/admob/android/test-ads
    -->
    <!-- Ad size is set programmatically in MainActivity.kt (adaptive banners need
         the screen width at runtime, which isn't expressible as a static XML value). -->
    <com.google.android.gms.ads.AdView
        android:id="@+id/adView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        ads:adUnitId="ca-app-pub-3940256099942544/6300978111" />

</LinearLayout>
